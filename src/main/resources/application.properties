spring.application.name=BLPS

spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5433/studs}
spring.datasource.username=${DB_USER:s413022}
spring.datasource.password=${DB_PASSWORD:1i3O5V9ts2y1GN7M}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:create}
spring.jpa.properties.hibernate.format_sql=true

server.port=${SERVER_PORT:8080}

jwt.secret=${JWT_SECRET:}
jwt.expiration=PT15M
jwt.issuer=blps

security.users.xml-path=classpath:security/users.xml