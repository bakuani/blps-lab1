spring.application.name=BLPS

spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5433/studs}
spring.datasource.username=${DB_USER:s413022}
spring.datasource.password=${DB_PASSWORD:1i3O5V9ts2y1GN7M}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:create}
spring.jpa.properties.hibernate.format_sql=true

server.port=${SERVER_PORT:8080}

jwt.secret=${JWT_SECRET:01234567890123456789012345678901}
jwt.expiration=PT15M
jwt.issuer=blps

security.users.xml-path=classpath:security/users.xml

rabbitmq.host=${RABBITMQ_HOST:localhost}
rabbitmq.port=${RABBITMQ_PORT:5672}
rabbitmq.username=${RABBITMQ_USER:guest}
rabbitmq.password=${RABBITMQ_PASSWORD:guest}
rabbitmq.vhost=${RABBITMQ_VHOST:/}

app.jms.tariff-change-queue=${TARIFF_CHANGE_QUEUE:tariff.change.requests}
app.jms.feature-disable-queue=${FEATURE_DISABLE_QUEUE:feature.disable.requests}
app.jms.monthly-fee-queue=${MONTHLY_FEE_QUEUE:monthly.fee.requests}
app.scheduler.monthly-fee-cron=${MONTHLY_FEE_CRON:*/30 * * * * *}
app.scheduler.monthly-fee-cycle-pattern=${MONTHLY_FEE_CYCLE_PATTERN:yyyy-MM-dd-HH:mm}

app.scheduler.retry-cron=${RETRY_CRON:0 */1 * * * *}
eis.dolibarr.url=${EIS_DOLIBARR_URL:http://localhost:8081}
eis.dolibarr.api-key=${EIS_DOLIBARR_API_KEY:test_api_key}
eis.dolibarr.fail-closed=${EIS_DOLIBARR_FAIL_CLOSED:true}
eis.dolibarr.connect-timeout-ms=${EIS_DOLIBARR_CONNECT_TIMEOUT_MS:3000}
eis.dolibarr.read-timeout-ms=${EIS_DOLIBARR_READ_TIMEOUT_MS:5000}
eis.dolibarr.audit.enabled=${EIS_DOLIBARR_AUDIT_ENABLED:false}
eis.dolibarr.audit.connection-factory-jndi=${EIS_DOLIBARR_AUDIT_CF_JNDI:java:/eis/DolibarrConnectionFactory}
eis.dolibarr.audit.interaction-name=${EIS_DOLIBARR_AUDIT_INTERACTION:dolibarr.audit.operation}
